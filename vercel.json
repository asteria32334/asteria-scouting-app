{
  "buildCommand": "bash -c 'git clone https://github.com/flutter/flutter.git --depth 1 --branch stable ~/flutter && export PATH=\"$PATH:~/flutter/bin\" && flutter pub get && flutter build web --release'",
  "outputDirectory": "build/web",
  "installCommand": "echo 'Installing Flutter...'",
  "framework": null
}