{
  "buildCommand": "bash -c 'git clone --depth 1 https://github.com/flutter/flutter.git ~/flutter && export PATH=\"$PATH:~/flutter/bin\" && flutter pub get && flutter build web --release'",
  "outputDirectory": "build/web",
  "installCommand": "echo 'Flutter kuruluyor...'", 
  "framework": null
}